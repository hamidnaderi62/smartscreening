[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = ["Lib", "Scripts", "media", "static", "staticfiles"]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501"]
